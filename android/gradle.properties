org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Pins Gradle to JDK 21 instead of whichever JDK 25 install is being picked
# up by default (JAVA_HOME / the "Program Files\Android\Android Studio"
# copy). Gradle 8.14's bundled Kotlin DSL compiler throws
# "IllegalArgumentException: 25.0.2" trying to parse a JDK 25 version
# string when compiling build.gradle.kts — it doesn't understand that
# format yet. JDK 21 (the other Android Studio install already on this
# machine) doesn't have this problem.
# NOTE: if this path is different on your machine, update it to wherever
# your JDK 21 (or 17) is installed.
org.gradle.java.home=C:\\(E) Folder\\AndroidStudio\\jbr